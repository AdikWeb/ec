import {storeDispatch} from "@/redux/store.facade";

export function CardLS() {
    const addToCard = (product: any, count: number)=>{

    }

    const getCard = ()=>{

    }

    return {
        addToCard,
        getCard
    }
}